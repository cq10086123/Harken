# 更新日志

> 发版时 CI 会自动取用对应版本的 `## v<版本号>` 小节作为 Release 说明；
> 找不到小节则回退为「上一个 tag → 当前提交」的提交列表。
> **新增版本时，请在最上方按同样格式补一节。**

## v1.7.9 — 2026-09-18

### 新增

- **自动识别导入的是「有声书」还是「音乐」**，并用对应方式排序与展示：
  有声书按集数（第1集 → 第2集 → …），音乐按轨道号 / 歌名。
  识别只用**在没有标签的文件上依然可靠**的信号：标题是否为「第X集 / EP01 / 001、」
  这类集数写法、单集平均时长、是否带真实歌手标签与轨道号——本地文件常常没有
  完整标签，所以不会依赖它们单独判定。识别结果显示在专辑详情页的「67首 · 音乐」位置，
  方便你确认识别得对不对。
- **排序偏好改为按专辑分别记忆**。以前是全局的：在某个有声书专辑里选了「集数」排序，
  所有音乐专辑也会跟着变成集数排序。


### 修复

- **本地专辑点进去显示 0 首**（专辑列表上明明写着 67 首）。集数排序的提取函数里，
  兜底用的正则没有捕获组，却直接取 `group(1)`——标题里含数字但不含「第X集 / EP」
  这类格式时抛 `RangeError`，异常被详情页外层捕获后整张专辑变成空列表。
  这也解释了为什么有声书专辑一切正常（标题都是「第1集」，走的是有捕获组的分支），
  而音乐专辑全空。
- **排序失败不再让专辑变空**：排序出错时回退为原始顺序并留下日志，不再静默清空。
- 补了集数提取与专辑排序的回归测试（覆盖「含数字的普通歌名」这类会触发上述异常的数据）。

## v1.7.8 — 2026-09-18

### 修复

- **扫描成功、歌也入库了，但列表页看不到本地歌**。根因是各列表页只读服务器分页
  数据，本地歌存在另一处，两边互不知道；更麻烦的是本地读取失败时被 `catch (_)`
  静默吞成空列表——页面干净地空白，连条日志都没有。
- **专辑页不再「与远端同名则跳过」**。同名不代表同一张专辑（本地文件夹名与服务器
  专辑重名很常见），跳过会让你以为扫进来的歌没进专辑页。现在本地专辑一律追加，
  并按本地组名去重（反复刷新不会重复）。
- **修掉两处会悄悄影响分页的隐患**：本地条目此前直接被混进远端列表，而列表的
  「还有更多」判定用的正是该列表长度，混入后会导致远端页少加载。改为本地条目
  单独存放、展示时拼接。

### 新增

- **本地音源内容全面进入各列表页**：
  - 歌曲列表：本地歌与在线歌一并展示，可直接播放
  - 专辑列表 / 专辑详情
  - 最近播放：本地播放记录（按最后播放时间倒序）
  - 首页：「最近播放 / 我的收藏」并入本地内容
- 新增 `LocalLibraryService`：本地曲库的唯一读取入口，专辑名回退链、文件夹名
  解析、最近播放排序只此一处实现，避免以后又出现「这个页面接了、那个没接」。
  读取失败一律留下日志，不再静默空白。

### 说明

- 歌手页 / 风格页暂未并入本地：本地文件多为「未知艺术家」且没有流派标签，接了也
  只有一个分组，意义不大（等扫描器能读到这些标签再补）。
- 文件夹页仍是服务器文件浏览器；本地目录的管理在 设置 → 音源管理。
- DLNA 投屏本地歌仍不可用：需要 App 内置本地 HTTP 服务供局域网设备拉流，属独立需求。

## v1.7.7 — 2026-09-18

### 修复

- **本地导入的歌曲一直转圈加载不出来**，并误报「网络缓慢，正在缓冲…」。
  本地文件在「FFmpeg 解码」这条路径上缺少取源分支，被当成云端歌曲去请求服务器
  地址——而该地址在服务器上并不存在，所以永远加载不完。现在本地文件由播放器直接
  读本地路径，不经过任何网络。
- **本地文件不再提示「网络缓慢」**。本地播放与网络无关，缓冲慢只可能是文件或解码
  问题，提示语不再误导排查方向。
- **本地文件不再被送进服务器转码**。转码是服务器行为，本地文件没有服务端会话；
  此前大体积本地无损文件可能因超过阈值被误送去转码（必然失败）。

### 优化

- **本地 m4a / aac / mp4 恢复系统解码（硬件解码）**。原本「编码未知 +
  可疑容器一律首发 FFmpeg」的保守策略改为**只对在线歌曲生效**——它本来是
  为远程流设计的（远程流常常拿不到编码标记，设备解码器可能静默失败），
  而本地文件的格式由扩展名确定，系统解码器处理本地 m4a 是标准能力。
  走 FFmpeg 软解只会白耗 CPU 与电量。桌面端与黑名单格式（dsf / ape /
  wma 等系统解不了的）仍然走 FFmpeg。
- **本地歌曲不再为读取元数据请求服务器**。本地歌没有服务端元数据，此前
  构建播放队列时会逐首发出 `/track/metadata` 请求（必然失败），拖慢队列
  构建，现已直接跳过。

### 说明

- 本地歌曲在「FFmpeg 软解码」模式下同样可以正常播放。
- 本地文件与飞牛在线曲库可以混在同一个播放队列里，各自走各自的链路。

## v1.7.6 — 2026-09-18

### 修复

- **飞牛歌曲播放到第 3 秒跳回 0 秒重播**。这类歌曲（编码信息未知的 m4a / aac / mp4 / mkv 等）
  现在一开始就用 FFmpeg 引擎播放，不再「先用系统解码器播、3 秒后中途切换引擎」——
  切换瞬间进度跳转失败正是重播的根源。
- **移除「无声看门狗」**。它无法真正检测静音（系统解码器不提供音量接口），只能按
  「编码未知 + 可疑容器」去猜，播放完全正常时也会被强制切换引擎。改为在播放前按格式与
  编码决定引擎，不再有中途打扰。
- **收藏页看不到本地收藏**。此前登录飞牛后，收藏页只读服务端收藏，本地收藏被无视；
  现在无论是否登录都会合并显示。
- **新拷贝进手机的音频文件夹扫不出歌**。扫描改为「系统媒体库 + 文件系统」双路线并集，
  系统媒体库尚未索引的新文件也能扫到（旧文件夹一直能扫是因为早已被索引）。

### 新增

- **解码引擎设置**（设置 → 功能 → 解码引擎）：手动指定解码器，不再完全依赖自动判断。
  - **自动**（默认）：按格式与编码智能选择，风险格式首发 FFmpeg
  - **系统解码（硬件解码）**：全部走系统解码器，省电
  - **FFmpeg 软解码**：全部走 FFmpeg，兼容性最好、必定出声
  - 切换后**立即生效**：自动重载当前曲并保留播放进度
- 单曲仍可在歌曲信息面板点「解码」标签单独指定引擎，优先级高于全局设置。

### 说明

- FFmpeg 软解码模式下不再使用服务器转码（直连原始流；无损大文件的流量会更大）。
- 解码**真的失败**时仍会自动兜底切到 FFmpeg 重试一次，避免卡死在坏源上（仅在失败时触发，
  不做预防性切换）。
- 桌面端（Windows / macOS / Linux）固定使用 FFmpeg——系统无对应解码实现。
- 引擎选择优先级：单曲手动 > 全局设置 > 自动路由 > 解码失败兜底。
